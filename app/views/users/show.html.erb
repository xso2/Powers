<%= render partial:"meal_records/meal_record", locals: { meal_record: @meal_record } %>

<%= render partial:"body_makes/body_make", locals: { body_make: @body_make } %>

<%= month_calendar do |date| %>
  <%= date %>
<% end %>