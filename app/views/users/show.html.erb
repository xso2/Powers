<%= month_calendar(events: @body_makes) do |date, body_makes| %>
  <%= date %>

  <% body_makes.each do |body_make| %>
    <div class="weight">
      <%= link_to body_make_path(body_make.id) do %>
        <%= body_make.weight %>kg
      <% end %>
    </div>
  <% end %>

    <%# <% @meal_records.each do |meal_record|%> 
    <%# <div class="weight"> %>
      <%# <%= link_to "食事内容", meal_record_path(meal_record.id) %> 
    <%# </div> %>
  <%# <% end %> 
<% end %>

<%# <%= line_chart BodyMake.group_by_day(:start_time, :weight).count %> 